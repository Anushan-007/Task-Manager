import { HasDirectiveDirective } from './has-directive.directive';

describe('HasDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HasDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
